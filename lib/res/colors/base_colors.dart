import 'package:flutter/material.dart';

abstract class BaseColors {
  // theme colors
  MaterialColor get themePrimary;
  MaterialColor get themeSecondary;
  // principal colors
  Color get primary;
  Color get secondary;
  Color get alt1;
  Color get alt2;
  Color get dark;
}
